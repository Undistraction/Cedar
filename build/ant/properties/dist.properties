#Distribution Structure
[output](dist)dir=${basedir}/dist
[output](dist)src.dir=${[output](dist)dir}/src
[output](dist)bin.dir=${[output](dist)dir}/bin
[output](dist)docs.dir=${[output](dist)dir}/docs
[output](dist)asdocs.dir=${[output](dist)docs.dir}/asdocs
[output](dist)swc=${[output](dist)bin.dir}/{project.name.short}.swc
[output](dist)deploy.dir=${[output](dist)dir}/deploy
[output](dist)zip=${[output](dist)dir}/${project.name.short}.zip