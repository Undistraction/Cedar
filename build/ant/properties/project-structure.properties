#Project Structure
src.dir = ${basedir}/src
libs.dir = ${basedir}/libs
bin.dir=${basedir}/bin
build.dir=${basedir}/build
build.assets.dir=${build.dir}/assets
build.docs.dir=${build.assets.dir}/docs
output.dir = ${basedir}/deploy
assets.dir = ${basedir}/assets
assets.runtime.dir = ${assets.dir}/runtime
assets.embedded.dir = ${assets.dir}/embedded
wrapper.template.dir = ${basedir}/html-templates
